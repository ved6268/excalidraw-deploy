# Managed by Inframan — generated from console config. Edits here are the source of truth.
version: v1
# immutable — managed by Inframan, do not edit
application: excalidraw
# immutable — managed by Inframan, do not edit
namespace: excalidraw
services:
    web:
        type: web
        port: 80
        domain: ved.inframan.cloud
        scaling:
            type: concurrency
            min_replicas: 1
            max_replicas: 1
            target: 100
        resources:
            cpu_request: 100m
            memory_request: 256Mi
            cpu_limit: 500m
            memory_limit: 512Mi
        build:
            method: docker
            dockerfile: ./Dockerfile
